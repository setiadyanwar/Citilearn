/**
 * File Upload Constants
 * Centralized configuration for file uploads
 */

// Max file size in bytes
export const MAX_THUMBNAIL_SIZE_MB = 5;
export const MAX_THUMBNAIL_SIZE_BYTES = MAX_THUMBNAIL_SIZE_MB * 1024 * 1024;

export const MAX_ANSWER_IMAGE_SIZE_MB = 2;
export const MAX_ANSWER_IMAGE_SIZE_BYTES = MAX_ANSWER_IMAGE_SIZE_MB * 1024 * 1024;

export const ALLOWED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp'];
